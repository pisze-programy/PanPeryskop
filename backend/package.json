{
  "name": "panperyskop-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "tsx --test test/seed*.test.ts",
    "gen:templates": "tsx scripts/gen-templates.ts",
    "db:migrate": "wrangler d1 migrations apply panperyskop-db",
    "db:create": "wrangler d1 execute panperyskop-db --file=./schema/schema.sql",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^1.3.0",
    "hono": "^4.6.0",
    "nanoid": "^6.0.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5",
    "@types/node": "^26.2.0",
    "tsx": "^4",
    "typescript": "^5.7.0",
    "wrangler": "^4.118.0"
  }
}
