// Admin custom CSS (served at /admin/static/css/admin.css). Tabler is the base;
// only the gaps Tabler doesn't cover live here.
export const ADMIN_CSS = String.raw`
.icon-tiny { width: 0.875rem; height: 0.875rem; }
.icon-xs { width: 0.75rem; height: 0.75rem; }
.pp-col-limit { max-width: 20rem; }
.pp-drop-min { min-width: 230px; }
.pp-progress-min { min-width: 120px; }
.pp-progress-xs { min-width: 80px; }
.pp-cursor-pointer { cursor: pointer; }
#ppMap { min-height: 320px; }
`;
